<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ShapeableImageView -->
    <style name="ShapeAppearanceOverlay.App.circleImageView.15" parent="Base.Theme.TravelApp">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">15%</item>
    </style>

    <style name="ShapeAppearanceOverlay.App.circleImageView.50" parent="Base.Theme.TravelApp">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    <!-- ShapeableImageView end-->

    <!-- collapsing toolbar -->
    <style name="Theme.Base.CollapsingToolbar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- spinner style -->
    <style name="Widget.My.Spinner" parent="Widget.AppCompat.Spinner">
        <item name="android:overlapAnchor">false</item>
        <item name="overlapAnchor"> false</item> <!-- true: 드롭다운 메뉴의 표시위치가 스피너와 겹칠 수 있음, false:  드랍다운 메뉴가 스피너 아래로 고정 -->
        <item name="android:spinnerMode">dropdown</item> <!-- dropdown: 메뉴가 드랍다운 형식으로 표시, dialog: 메뉴가 팝업형태로 표시 -->
        <item name="android:dropDownWidth">match_parent</item> <!-- dropDownWidth: 드랍다운 메뉴의 가로길이를 설정 -->
        <item name="android:background">@drawable/bg_spinner</item>
    </style>

    <!-- TabLayout 텍스트 -->
    <style name="merchandise_tab_text_appearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontStyle">normal</item>
        <item name="android:paddingBottom">17dp</item>
        <item name="android:paddingTop">17dp</item>
        <item name="android:includeFontPadding">false</item>
    </style>
    <style name="tabTextSizeBold" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">16sp</item>
        <item name="android:textStyle">bold</item>
    </style>


    <!-- bottomSheetFragment border 20dp 전용 테마 -->
    <style name="AppBottomSheetDialogBorder20WhiteTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalBorder20WhiteStyle</item>
        <item name="android:windowIsFloating">false</item>
    </style>

    <style name="AppModalBorder20WhiteStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bottom_sheet_white_corner</item>
    </style>

    <!-- dialog -->
    <style name="CustomDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>